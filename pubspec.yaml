name: biteo_vendor_panel
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.1.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.1.0
  firebase_auth: ^5.7.0
  firebase_messaging: ^15.0.0

  flutter_local_notifications: ^17.0.0
  audioplayers: ^5.2.1
  image_picker: ^1.1.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/alert.mp3